{
  "expo": {
    "plugins": [
      "expo-system-ui",
      ["expo-camera", {
        "cameraPermission": "This tests the multiple variants of defining plugins"
      }],
      ["./plugins/schema", {
        "name": ""
      }]
    ]
  }
}
